# Windows 定时任务配置说明

## 功能

每天自动运行 `fetch_news.py` 抓取最新 AI 资讯，更新 `js/data.js` 数据文件。

## 快速安装

### 方法一：使用安装脚本（推荐）

1. 右键点击 **PowerShell** → 选择 **"以管理员身份运行"**
2. 切换到项目目录：
   ```powershell
   cd C:\Users\Administrator\WorkBuddy\2026-05-15-task-2
   ```
3. 执行安装脚本：
   ```powershell
   .\install_task.ps1
   ```

### 方法二：手动创建（任务计划程序）

1. 打开任务计划程序（`taskschd.msc`）
2. 右侧点击 **"创建任务"**
3. 填写配置：
   - **名称**：`AI资讯自动抓取`
   - **安全选项**：勾选 `不管用户是否登录都要运行`，勾选 `使用最高权限运行`
   - **触发器**：新建 → 每天 → 08:00:00
   - **操作**：新建 → 程序：`cmd.exe`
     参数：
     ```
     /c set PYTHONUTF8=1 && cd /d C:\Users\Administrator\WorkBuddy\2026-05-15-task-2 && python.exe fetch_news.py >> logs\scheduler.log 2>&1
     ```
   - **设置**：勾选 `如果过了计划开始时间，立即启动任务`

## 文件说明

| 文件 | 说明 |
|------|------|
| `install_task.ps1` | 自动安装脚本（需管理员权限） |
| `uninstall_task.ps1` | 卸载脚本 |
| `logs\scheduler.log` | 定时任务运行日志 |

## 常用管理命令

```powershell
# 查看任务状态
Get-ScheduledTask -TaskName "AI资讯自动抓取"

# 手动运行任务
Start-ScheduledTask -TaskName "AI资讯自动抓取"

# 查看上次运行结果
Get-ScheduledTaskInfo -TaskName "AI资讯自动抓取"

# 禁用任务
Disable-ScheduledTask -TaskName "AI资讯自动抓取"

# 启用任务
Enable-ScheduledTask -TaskName "AI资讯自动抓取"

# 删除任务
Unregister-ScheduledTask -TaskName "AI资讯自动抓取" -Confirm:$false
```

## 修改运行时间

打开任务计划程序 → 找到 `AI资讯自动抓取` → 右键 **属性** → **触发器** 选项卡 → 编辑触发时间。

建议时间：
- **08:00** - 早上上班前完成更新
- **12:00** - 午间更新
- **20:00** - 晚间更新

可设置多个触发器实现一天多次更新。

## 故障排查

### 任务没有运行

1. 检查 Python 是否在 PATH 中：
   ```powershell
   python --version
   ```
   如果找不到，修改 `install_task.ps1` 第 7 行，改为完整路径：
   ```powershell
   $PythonPath = "C:\Python39\python.exe"
   ```

2. 查看运行日志：
   ```powershell
   notepad C:\Users\Administrator\WorkBuddy\2026-05-15-task-2\logs\scheduler.log
   ```

3. 检查任务计划程序中的 `上次运行结果` 字段

### 常见错误码

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 1 | 一般性失败（检查 Python 脚本是否有误） |
| 0x41301 | 任务正在运行 |
| 0x41303 | 任务未被调度触发 |
| 0x800710E0 | 任务被禁用 |
